# Updating this mirror

Do not hand-edit generated Markdown files. Regenerate the corpus from the Multi Theft Auto MediaWiki API so source metadata, links, and indexes remain consistent.

When reporting a documentation issue, include the source URL from the affected file's frontmatter.
