# MTA Wiki Markdown Mirror

English-only, generated from the public Multi Theft Auto MediaWiki API. Each Markdown file preserves its source URL, revision ID, categories and stable document ID in YAML frontmatter. Internal links use `mta://` paths into this corpus.
